# --- Supabase (client-side; safe to ship — protected by Row Level Security) ---
# Canonical backend = the "AVDP-Dashboard" project, which holds all migrations,
# seed data, RLS, auth users, the pg_cron digest, and the `advisor` edge function.
# Set these SAME values in the Cloudflare project's Build env vars for production.
VITE_SUPABASE_URL="https://ezflemlmghvxahvrcrxd.supabase.co"
VITE_SUPABASE_ANON_KEY="sb_publishable_oDd8s2g_Dt3l3FUkMSleIw_uItUcf4l"

# GEMINI_API_KEY is NOT a frontend variable. The AI advisor runs as a Supabase
# Edge Function (supabase/functions/advisor); set the key as a function secret:
#   supabase secrets set GEMINI_API_KEY=...
# Without it, the advisor returns a graceful fallback response.
